apiVersion: apps/v1
kind: deployment
metadata:
  name: nginx
  lables:
    app: nginx
spec:
  replicas: 1
  selector: 
    matchLables: 
      name: nginx
  template:
    metadata:
      name: nginx
      lables: 
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx
        resources:
          requests:
            cpu: "50m"
          limits:
            cpu: "150"
        ports:
        - containerPort: 80

---

apiVersion: v1
kind: service
metadata: 
  name: nginx
  lables:
    app: nginx
spec:
  type: LoadBalencer
  - port: 80
    target: 80
  selector:
    matchLables:
      app: nginx
