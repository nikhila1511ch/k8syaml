apiVersion: v1
kind: Pod
metadata:
  name: init-pod
  namespace: frontend
  labels: 
    env: frontend
spec:
  initContainers:
    - name: init-pod
      image: alpine
      command: ["sh","-c"]
      args: ["echo 'this is init container';sleep 20"]
      
  containers: 
    - name: pod-1
      image: nginx
      command: ["sh","-c"]
      args: ["echo 'this is main container';sleep 3600"]
